"""Stub of mpos.board.

The real firmware exposes exactly one submodule, named after the hardware id,
holding the board wiring. The app finds the S button by walking this.
"""

from . import fri3d_2026  # noqa: F401
